

const PageContainer = ({children}) => {
    return (
        <div className="pt-14 px-5 bg-black text-white h-full min-h-screen">
          {children}
        </div>
    )
}

export default PageContainer